import React from "react";
import "./Dashboard.css";
import { Image } from "react-bootstrap";

//if auth, show dash

function Dashboard(user) {
    return <div>
            <h2>This will show the users dashboard when logged in</h2>
    </div>
}

export default Dashboard;